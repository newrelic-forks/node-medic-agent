// Package providerid parses the `spec.providerID` URI on a Kubernetes Node
// and returns the cloud-specific instance identifier (AWS instance-id or
// Azure VM name) per spec FR-2.
package providerid
